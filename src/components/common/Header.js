import { Marquee } from './Marquee'

const Header = () => {
  return (
    <Marquee
      text="BY SKOLKOVO × MTS"
      height={20}
      speed={20}
      offset={20}
    />
  )
}

export default Header
